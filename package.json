{
	"name": "vue-i18n-manage",
	"displayName": "vue-i18n-manage",
	"description": "A plguin based on vscode that is targeted for international file management.",
	"publisher": "vue-i18n-manage",
	"version": "0.1.0",
	"engines": {
		"vscode": "^1.33.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onLanguage:vue",
		"onLanguage:javascript",
		"onLanguage:typescript"
	],
	"keywords": [
		"vue",
		"vue-i18n",
		"vue i18m",
		"i18n",
		"vue-i18n-manage",
		"i18n manage"
	],
	"icon": "imgs/logo.png",
	"repository": {
		"type": "git",
		"url": "https://github.com/flyer-ui/vue-i18n-manage.git"
	},
	"homepage": "https://github.com/flyer-ui/vue-i18n-manage",
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "extension.config",
				"title": "Configure the path to the data source.",
				"icon": {
					"light": "imgs/logo.svg",
					"dark": "imgs/logo.svg"
				}
			}
		],
		"menus": {
			"editor/title": [
				{
					"when": "editorFocus",
					"command": "extension.config",
					"group": "navigation"
				}
			]
		},
		"configuration": {
			"type": "object",
			"title": "vue-i18n-manage",
			"properties": {
				"vue-i18n-manage.path": {
					"type": "string",
					"default": "",
					"description": "Path to recording the data source."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"postinstall": "node ./node_modules/vscode/bin/install",
		"test": "npm run compile && node ./node_modules/vscode/bin/test"
	},
	"devDependencies": {
		"typescript": "^3.3.1",
		"vscode": "^1.1.28",
		"tslint": "^5.12.1",
		"@types/node": "^10.12.21",
		"@types/mocha": "^2.2.42"
	},
	"dependencies": {
		"deepmerge": "^3.2.0"
	}
}
